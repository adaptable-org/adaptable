<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'partials/head' %>
  </head>

  <body class="bg-adpt-navy">
    <div class="container bg-adpt-navy max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <%= render 'partials/header' %>
      <div class="max-w-3xl mx-auto">
        <article class="prose prose-lg text-gray-500 mx-auto bg-white">
          <%= yield %>
        </article>
      </div>
      <%= render 'partials/footer' %>
    </div>
  </body>
</html>
